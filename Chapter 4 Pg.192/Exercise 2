Write a program that reads from the console the radius "r" of a circle and prints its perimeter and area.

namespace CircleJerk
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.Write("Enter the radius of your circle to calculate its" +
                " perimeter and area: ");
            double radius = double.Parse(Console.ReadLine());
            //Saves the user's input as a 32 bit integer.

            double area = Math.PI * (radius * radius);
            double perimeter = 2 * Math.PI * radius;
            //calculates the area and perimeter

            Console.WriteLine("The area of the circle is {0} and the perimeter" +
                " is {1}.", area, perimeter);
        }
    }
}
